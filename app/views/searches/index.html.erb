<% if notice %>
	<aside id=“notice”><%= notice %></aside>
<% end %>

<h1>Your Pragmatic Catalog</h1>
	<ul class=“catalog”>
		<% @schools.each do |school| %>
			<li>
				<%= image_tag(school.image_url) %>
			<h2><%= school.name %></h2>
			<p>
				<%= sanitize(school.description) %>
			</p>
			<div class=“price”>
				<%= number_to_currency(school.price) %>
			</div>
			</li>
		<% end %>
	</ul>